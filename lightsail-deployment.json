{
  "serviceName": "lobby-proto",
  "containers": {
    "app": {
      "image": "thealligator/lobby-proto-app:latest",
      "ports": {
        "3000": "HTTP"
      },
      "environment": {
        "NODE_ENV": "production",
        "DATABASE_URL": "postgresql://postgres@localhost:5432/lobby_cms"
      }
    }
  },
  "publicEndpoint": {
    "containerName": "app",
    "containerPort": 3000,
    "healthCheck": {
      "healthyThreshold": 2,
      "unhealthyThreshold": 2,
      "timeoutSeconds": 5,
      "intervalSeconds": 30,
      "path": "/",
      "successCodes": "200-499"
    }
  }
}
